import React from 'react';
import ReactDOM from 'react-dom';
import './styles/App.css'; // Ganti dengan path ke CSS yang sudah diatur
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
